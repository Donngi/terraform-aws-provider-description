#---------------------------------------------------------------
# Amazon Network Flow Monitor スコープ
#---------------------------------------------------------------
#
# Amazon Network Flow Monitor のスコープ（Scope）をプロビジョニングするリソースです。
# スコープは、ネットワークフローのモニタリング対象となる AWS アカウントや
# リージョンの集合を定義します。Network Flow Monitor はスコープを使用して、
# 対象リソース間のネットワークフローデータを収集・分析します。
#
# AWS公式ドキュメント:
#   - Amazon Network Flow Monitor とは:
#       https://docs.aws.amazon.com/network-flow-monitor/latest/userguide/what-is-network-flow-monitor.html
#   - スコープの管理:
#       https://docs.aws.amazon.com/network-flow-monitor/latest/userguide/network-flow-monitor-scopes.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/networkflowmonitor_scope
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_networkflowmonitor_scope" "example" {
  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # モニタリング対象設定
  #-------------------------------------------------------------

  # target (Required)
  # 設定内容: Network Flow Monitor のモニタリング対象となる AWS アカウントと
  #           リージョンの組み合わせを指定するブロックです。
  # 注意: 複数のターゲットを指定する場合は、target ブロックを繰り返し記述します。
  target {

    # region (Required)
    # 設定内容: モニタリング対象リソースが存在する AWS リージョンを指定します。
    # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
    region = "ap-northeast-1"

    #-------------------------------------------------------------
    # ターゲット識別子設定
    #-------------------------------------------------------------

    # target_identifier (Optional)
    # 設定内容: モニタリング対象を識別するための情報を指定するブロックです。
    #           ターゲットの種別とアカウント ID を組み合わせて対象を特定します。
    target_identifier {

      # target_type (Required)
      # 設定内容: モニタリング対象のターゲット種別を指定します。
      # 設定可能な値:
      #   - "ACCOUNT": AWS アカウントを対象とする
      target_type = "ACCOUNT"

      #-------------------------------------------------------------
      # ターゲット ID 設定
      #-------------------------------------------------------------

      # target_id (Optional)
      # 設定内容: モニタリング対象の具体的な識別情報を指定するブロックです。
      #           アカウント ID を使用してターゲットを一意に識別します。
      target_id {

        # account_id (Required)
        # 設定内容: モニタリング対象の AWS アカウント ID を指定します。
        # 設定可能な値: 12桁の AWS アカウント ID（例: "123456789012"）
        account_id = "123456789012"
      }
    }
  }

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルの default_tags 設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "example-networkflowmonitor-scope"
    Environment = "production"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を指定するブロックです。
  # 設定可能な値: "30s", "5m", "2h" などの duration 形式
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成操作のタイムアウト時間を指定します。
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    create = "30m"

    # update (Optional)
    # 設定内容: リソース更新操作のタイムアウト時間を指定します。
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    update = "30m"

    # delete (Optional)
    # 設定内容: リソース削除操作のタイムアウト時間を指定します。
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    delete = "30m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: Network Flow Monitor スコープの ID
# - scope_arn: スコープの Amazon Resource Name (ARN)
# - scope_id: スコープの一意識別子
# - tags_all: プロバイダーの default_tags 設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
