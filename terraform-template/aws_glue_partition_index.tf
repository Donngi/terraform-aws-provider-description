#---------------------------------------------------------------
# AWS Glue Partition Index
#---------------------------------------------------------------
#
# AWS Glue データカタログのテーブルにパーティションインデックスをプロビジョニングするリソースです。
# パーティションインデックスを使用することで、テーブルに対するクエリを最適化し、
# パーティションのフィルタリングパフォーマンスを向上させることができます。
# 最大3つのパーティションインデックスをテーブルに作成できます。
#
# AWS公式ドキュメント:
#   - パーティションインデックスの作成: https://docs.aws.amazon.com/glue/latest/dg/partition-indexes.html
#   - CreatePartitionIndex API: https://docs.aws.amazon.com/glue/latest/webapi/API_CreatePartitionIndex.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/glue_partition_index
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_glue_partition_index" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # database_name (Required)
  # 設定内容: テーブルメタデータが存在するメタデータデータベースの名前を指定します。
  # 設定可能な値: 有効なGlueデータカタログデータベース名（Hive互換のためすべて小文字）
  database_name = "example_database"

  # table_name (Required)
  # 設定内容: パーティションインデックスを作成するテーブルの名前を指定します。
  # 設定可能な値: 有効なGlueデータカタログテーブル名（Hive互換のためすべて小文字）
  table_name = "example_table"

  #-------------------------------------------------------------
  # カタログ設定
  #-------------------------------------------------------------

  # catalog_id (Optional)
  # 設定内容: テーブルが存在するカタログのIDを指定します。
  # 設定可能な値: 有効なAWSアカウントID（12桁の数字）
  # 省略時: 現在のAWSアカウントIDが使用されます
  catalog_id = null

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # パーティションインデックス設定
  #-------------------------------------------------------------

  # partition_index (Required)
  # 設定内容: パーティションインデックスの設定ブロックです。
  # 関連機能: Glue パーティションインデックス
  #   テーブルに定義されたパーティションキーのサブセットを指定してインデックスを作成します。
  #   String、Numeric、Date型をサポートし、=、>、>=、<、<= 演算子で利用可能です。
  #   - https://docs.aws.amazon.com/glue/latest/dg/partition-indexes.html
  # 注意: 1つのリソースブロックにつき1つのパーティションインデックスのみ指定可能です（max_items=1）
  partition_index {

    # index_name (Optional)
    # 設定内容: パーティションインデックスの名前を指定します。
    # 設定可能な値: 1〜255文字の文字列
    index_name = "example_index"

    # keys (Optional)
    # 設定内容: パーティションインデックスに使用するキーのリストを指定します。
    # 設定可能な値: テーブルに定義されたパーティションキー名の文字列リスト
    # 注意: 指定するキーは対象テーブルの partition_keys に定義されているものである必要があります
    keys = ["my_column_1", "my_column_2"]
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を指定します。
  timeouts {

    # create (Optional)
    # 設定内容: パーティションインデックス作成操作のタイムアウト時間を指定します。
    # 設定可能な値: "10m"、"1h" などのGo duration形式の文字列
    # 省略時: プロバイダーのデフォルトタイムアウト値が適用されます
    create = "10m"

    # delete (Optional)
    # 設定内容: パーティションインデックス削除操作のタイムアウト時間を指定します。
    # 設定可能な値: "10m"、"1h" などのGo duration形式の文字列
    # 省略時: プロバイダーのデフォルトタイムアウト値が適用されます
    delete = "10m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: カタログID、データベース名、テーブル名、インデックス名を結合した識別子
#---------------------------------------------------------------
