#---------------------------------------------------------------
# AWS Service Catalog AppRegistry Attribute Group
#---------------------------------------------------------------
#
# AWS Service Catalog AppRegistry の属性グループを管理するリソースです。
# 属性グループはJSONで定義したメタデータをアプリケーションに付与するための
# キーバリューペアの集合で、アプリケーションに関連付けて利用します。
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/servicecatalogappregistry_attribute_group
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_servicecatalogappregistry_attribute_group" "example" {
  #-------------------------------------------------------------
  # 識別情報
  #-------------------------------------------------------------

  # name (Required)
  # 設定内容: 属性グループの名前
  # 設定可能な値: 英数字・ハイフン・アンダースコアからなる文字列
  # 省略時: 設定必須
  name = "example"

  # attributes (Required)
  # 設定内容: 属性グループを定義するJSONキーバリューペア（ネスト可能）
  # 設定可能な値: jsonencode() 関数で生成したJSON文字列
  # 省略時: 設定必須
  attributes = jsonencode({
    app   = "exampleapp"
    group = "examplegroup"
  })

  #-------------------------------------------------------------
  # 説明
  #-------------------------------------------------------------

  # description (Optional)
  # 設定内容: 属性グループの説明
  # 設定可能な値: 任意の文字列
  # 省略時: 説明なし
  description = "example description"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: 属性グループに付与するタグのマップ
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし（プロバイダーのdefault_tagsは引き継がれる）
  tags = {
    Name = "example"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn:      属性グループのARN
# - id:       属性グループのID
# - tags_all: プロバイダーの default_tags を含む全タグのマップ
#---------------------------------------------------------------
