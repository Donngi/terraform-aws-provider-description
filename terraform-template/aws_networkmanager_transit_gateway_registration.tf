#---------------------------------------------------------------
# AWS Network Manager Transit Gateway Registration
#---------------------------------------------------------------
#
# グローバルネットワークにトランジットゲートウェイを登録するリソースです。
# トランジットゲートウェイは任意のAWSリージョンに存在できますが、
# グローバルネットワークと同じAWSアカウントが所有している必要があります。
# 1つのトランジットゲートウェイは複数のグローバルネットワークに
# 登録することはできません。
#
# AWS公式ドキュメント:
#   - Transit Gateway Registrations: https://docs.aws.amazon.com/network-manager/latest/tgwnm/working-with-transit-gateways.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/networkmanager_transit_gateway_registration
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_networkmanager_transit_gateway_registration" "example" {
  #-------------------------------------------------------------
  # グローバルネットワーク設定
  #-------------------------------------------------------------

  # global_network_id (Required)
  # 設定内容: トランジットゲートウェイを登録するグローバルネットワークのIDを指定します。
  # 設定可能な値: 有効なNetwork Manager グローバルネットワークID
  # 省略時: 必須項目のため省略不可
  global_network_id = "network-0123456789abcdef0"

  # transit_gateway_arn (Required)
  # 設定内容: 登録するトランジットゲートウェイのARNを指定します。
  # 設定可能な値: 有効なEC2トランジットゲートウェイのARN
  # 省略時: 必須項目のため省略不可
  # 注意: 指定するトランジットゲートウェイはグローバルネットワークと同じAWSアカウントが
  #       所有している必要があります。
  transit_gateway_arn = "arn:aws:ec2:ap-northeast-1:123456789012:transit-gateway/tgw-0123456789abcdef0"

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "10m", "1h30m"）
    # 省略時: プロバイダーのデフォルトタイムアウト値を使用します。
    create = "10m"

    # delete (Optional)
    # 設定内容: リソース削除時のタイムアウト時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "10m", "1h30m"）
    # 省略時: プロバイダーのデフォルトタイムアウト値を使用します。
    delete = "10m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: グローバルネットワークIDとトランジットゲートウェイARNを
#       コンマ区切りで結合した値
#---------------------------------------------------------------
