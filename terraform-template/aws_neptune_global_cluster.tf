#---------------------------------------------------------------
# Amazon Neptune Global Cluster
#---------------------------------------------------------------
#
# Amazon Neptune グローバルクラスターをプロビジョニングするリソースです。
# グローバルクラスターは1つのプライマリリージョンと最大5つの読み取り専用
# セカンダリリージョンで構成されます。書き込み操作はプライマリリージョンの
# プライマリクラスターに直接発行し、Neptuneが専用インフラを使用して
# セカンダリリージョンへデータを自動的にレプリケートします。
#
# AWS公式ドキュメント:
#   - Neptune Global Database: https://docs.aws.amazon.com/neptune/latest/userguide/neptune-global-database.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/neptune_global_cluster
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_neptune_global_cluster" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # global_cluster_identifier (Required, Forces new resource)
  # 設定内容: グローバルクラスターの一意な識別子を指定します。
  # 設定可能な値: 文字列（英数字およびハイフンを使用した一意の識別子）
  global_cluster_identifier = "my-neptune-global-cluster"

  #-------------------------------------------------------------
  # エンジン設定
  #-------------------------------------------------------------

  # engine (Optional, Forces new resource)
  # 設定内容: このDBクラスターに使用するデータベースエンジン名を指定します。
  # 設定可能な値:
  #   - "neptune": Amazon Neptuneエンジン（現在の有効な値）
  # 省略時: Terraformはドリフト検出のみ実行。source_db_cluster_identifierと競合します。
  # 注意: source_db_cluster_identifierと競合するため、同時に指定できません。
  engine = "neptune"

  # engine_version (Optional)
  # 設定内容: グローバルデータベースのエンジンバージョンを指定します。
  # 設定可能な値: 有効なNeptuneエンジンバージョン（例: "1.2.0.0", "1.3.0.0"）
  # 省略時: AWSが適切なバージョンを自動選択します。
  # 注意: エンジンバージョンのアップグレードはすべてのクラスターメンバーの
  #       即時更新を引き起こします。
  engine_version = "1.2.0.0"

  #-------------------------------------------------------------
  # ソースクラスター設定
  #-------------------------------------------------------------

  # source_db_cluster_identifier (Optional)
  # 設定内容: 作成時にグローバルクラスターのプライマリDBクラスターとして使用する
  #           既存クラスターのARNを指定します。
  # 設定可能な値: 既存のNeptuneクラスターのARN
  # 省略時: 新規グローバルクラスターを作成します。
  # 注意: Terraformはこの値のドリフト検出を行いません。
  #       engineと競合するため、同時に指定できません。
  source_db_cluster_identifier = null

  #-------------------------------------------------------------
  # 暗号化設定
  #-------------------------------------------------------------

  # storage_encrypted (Optional, Forces new resource)
  # 設定内容: DBクラスターを暗号化するかどうかを指定します。
  # 設定可能な値:
  #   - true: 保存データを暗号化します。
  #   - false: 暗号化を無効にします。
  # 省略時: false（ただしsource_db_cluster_identifierが暗号化済みの場合はtrue）
  # 注意: Terraformは設定値が指定された場合のみドリフト検出を行います。
  storage_encrypted = false

  #-------------------------------------------------------------
  # 削除保護設定
  #-------------------------------------------------------------

  # deletion_protection (Optional)
  # 設定内容: グローバルクラスターの削除保護を有効にするかどうかを指定します。
  # 設定可能な値:
  #   - true: 削除保護を有効化。この値がtrueの場合、データベースを削除できません。
  #   - false: 削除保護を無効化。
  # 省略時: false
  deletion_protection = false

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用します。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  timeouts {
    # create (Optional)
    # 設定内容: グローバルクラスター作成時のタイムアウト時間を指定します。
    # 設定可能な値: Golangのduration形式の文字列（例: "5m", "10m", "1h"）
    # 省略時: 5分
    create = "5m"

    # update (Optional)
    # 設定内容: グローバルクラスターメンバー更新時のタイムアウト時間を指定します。
    # 設定可能な値: Golangのduration形式の文字列（例: "30m", "120m"）
    # 省略時: 120分（メンバーあたりの時間）
    update = "120m"

    # delete (Optional)
    # 設定内容: グローバルクラスターメンバー削除時のタイムアウト時間を指定します。
    # 設定可能な値: Golangのduration形式の文字列（例: "5m", "10m"）
    # 省略時: 5分（メンバーあたりの時間）
    delete = "5m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: グローバルクラスターのARN
#
# - global_cluster_members: グローバルクラスターメンバーを含むオブジェクトのセット
#     - db_cluster_arn: メンバーDBクラスターのARN
#     - is_writer: そのメンバーがプライマリDBクラスターかどうか
#
# - global_cluster_resource_id: グローバルデータベースクラスターのAWSリージョン固有の
#                               不変な識別子。DBクラスターのKMSキーにアクセスされるたびに
#                               AWS CloudTrailログエントリに記録されます。
#
# - status: グローバルクラスターの現在のステータス
#---------------------------------------------------------------
