#---------------------------------------------------------------
# AWS SESv2 Tenant
#---------------------------------------------------------------
#
# Amazon SES (Simple Email Service) v2のテナントをプロビジョニングするリソースです。
# テナントは、SESアカウント内でメール送信リソースを論理的に分離するコンテナです。
# 各テナントは独自のメールアイデンティティ、設定セット、テンプレート、
# レピュテーションメトリクスを持ち、異なる顧客やビジネスユニット間で
# メール送信アクティビティを完全に分離できます。
#
# AWS公式ドキュメント:
#   - SESテナント管理: https://docs.aws.amazon.com/ses/latest/dg/tenants.html
#   - CreateTenant API: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateTenant.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/sesv2_tenant
#
# Provider Version: 6.28.0
# Generated: 2026-02-08
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_sesv2_tenant" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # tenant_name (Required, Forces new resource)
  # 設定内容: テナントの名前を指定します。
  # 設定可能な値: AWSアカウントおよびリージョン内で一意の文字列
  # 注意: テナント名を変更すると、既存のテナントが削除され新しいテナントが作成されます。
  # 参考: https://docs.aws.amazon.com/ses/latest/dg/tenants.html
  tenant_name = "my-tenant"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 注意: テナントはリージョンスコープのリソースです。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: テナントに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "my-tenant"
    Environment = "production"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - sending_status: テナントの現在の送信ステータス。
#                   テナント作成時はENABLEDが設定されます。
#                   レピュテーションポリシーに基づき自動的にPausedになる場合があります。
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ。
#
# - tenant_arn: テナントのAmazon Resource Name (ARN)。
#
# - tenant_id: テナントの一意の識別子。
#---------------------------------------------------------------
