#---------------------------------------------------------------
# AWS VPC Lattice Service Network
#---------------------------------------------------------------
#
# Amazon VPC Latticeのサービスネットワークをプロビジョニングするリソースです。
# サービスネットワークは、サービスとVPCを論理的にグループ化するための境界として機能し、
# サービス間の接続性とアクセス制御を提供します。
# サービスネットワークに対してIAMポリシーを設定することで、
# クライアントのアクセス制御を一元管理できます。
#
# AWS公式ドキュメント:
#   - VPC Lattice概要: https://docs.aws.amazon.com/vpc-lattice/latest/ug/what-is-vpc-lattice.html
#   - サービスネットワーク: https://docs.aws.amazon.com/vpc-lattice/latest/ug/service-networks.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/vpclattice_service_network
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_vpclattice_service_network" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # name (Required)
  # 設定内容: サービスネットワークの名前を指定します。
  # 設定可能な値: 文字列（最大63文字、英数字とハイフンのみ使用可能）
  name = "example-service-network"

  #-------------------------------------------------------------
  # 認証設定
  #-------------------------------------------------------------

  # auth_type (Optional)
  # 設定内容: サービスネットワークに適用するIAMポリシーのタイプを指定します。
  # 設定可能な値:
  #   - "NONE"    - 認証なし（デフォルト）
  #   - "AWS_IAM" - AWS IAMによる認証を使用
  # 省略時: "NONE"
  # 注意: AWS_IAMを選択した場合は、別途aws_vpclattice_auth_policyリソースで
  #       アクセスポリシーを定義する必要があります。
  auth_type = "NONE"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "example-service-network"
    Environment = "production"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: サービスネットワークのAmazon Resource Name (ARN)
#
# - id: サービスネットワークのID
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
