#---------------------------------------------------------------
# AWS VPC IPAM Preview Next CIDR
#---------------------------------------------------------------
#
# VPC IPAMアドレスプールから次に割り当て可能なCIDRをプレビューするリソースです。
# プライベートIPv4のみに対応しており、実際のCIDR割り当ては行わず、プレビューのみを実施します。
# VPCやサブネットへの実際のCIDR割り当て前に、どのようなCIDRが割り当てられるかを
# 事前に確認する用途に使用します。
#
# AWS公式ドキュメント:
#   - VPC IPAM: https://docs.aws.amazon.com/vpc/latest/ipam/what-it-is-ipam.html
#   - IPAMプールからのCIDR割り当て: https://docs.aws.amazon.com/vpc/latest/ipam/allocate-cidrs-ipam.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/vpc_ipam_preview_next_cidr
#
# Provider Version: 6.28.0
# Generated: 2026-02-05
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_vpc_ipam_preview_next_cidr" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # ipam_pool_id (Required)
  # 設定内容: CIDRを割り当てたいIPAMプールのIDを指定します。
  # 設定可能な値: 有効なIPAMプールID
  # 関連機能: VPC IPAM
  #   IPAMプールはCIDRの集合を管理し、VPCやサブネットへのIP割り当てを自動化します。
  #   - https://docs.aws.amazon.com/vpc/latest/ipam/how-it-works-ipam.html
  ipam_pool_id = "ipam-pool-0123456789abcdef0"

  #-------------------------------------------------------------
  # CIDR設定
  #-------------------------------------------------------------

  # netmask_length (Optional)
  # 設定内容: IPAMプールからプレビューしたいCIDRのネットマスク長を指定します。
  # 設定可能な値: 0〜32の整数（IPv4の場合）
  # 省略時: IPAMプールの設定に基づいてCIDRが選択されます
  # 関連機能: CIDR割り当てサイズ
  #   ネットマスク長により、割り当てられるIPアドレスの範囲が決まります。
  #   例: /24 = 256アドレス、/28 = 16アドレス
  #   - https://docs.aws.amazon.com/vpc/latest/ipam/allocate-cidrs-ipam.html
  netmask_length = 28

  # disallowed_cidrs (Optional)
  # 設定内容: プールから除外したい特定のCIDR範囲を指定します。
  # 設定可能な値: CIDR形式の文字列のセット（例: "172.2.0.0/32"）
  # 省略時: CIDRの除外は行われません
  # 用途: 既に使用されているCIDRや予約済みのCIDRを除外し、
  #       競合を避けるために使用します。
  disallowed_cidrs = [
    "172.2.0.0/32"
  ]

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # 識別子設定
  #-------------------------------------------------------------

  # id (Optional, Computed)
  # 設定内容: プレビューの一意な識別子を指定します。
  # 設定可能な値: 文字列
  # 省略時: 自動的に生成されます
  # 注意: 通常、この属性は明示的に指定する必要はありません
  id = null
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - cidr: IPAMプールからプレビューされたCIDR。
#         実際の割り当てではなく、次に割り当て可能なCIDRの確認用。
#
# - id: プレビューの一意な識別子。
#       入力で指定しなかった場合は自動生成されます。
#---------------------------------------------------------------
