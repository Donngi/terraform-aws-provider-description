#-----------------------------------------------------------------------
# CloudFront Anycast IPリスト (aws_cloudfront_anycast_ip_list)
#-----------------------------------------------------------------------
#
# CloudFrontのAnycast静的IPリストを作成・管理します。
# Anycast静的IPは地理的に分散した固定IPアドレスのセットで、CloudFrontディストリビューションに
# 関連付けることで、ネットワークプロバイダーのアクセス許可リスト（allowlist）や
# トラフィック制御に使用できます。
#
# 主な用途:
# - ネットワークファイアウォールのアクセス許可リストへの登録
# - 承認済みアプリケーションとのトラフィック制御
# - 固定IPアドレスを必要とするパートナー連携
# - 複数のCloudFrontディストリビューションへの共有IPアドレスの提供
#
# 前提条件:
# - Anycast静的IPリストの利用にはCloudFrontサポートエンジニアリングへの事前申請が必要
# - ディストリビューションの料金クラスを「すべてのエッジロケーションを使用」に設定する必要あり
#
# Provider Version: 6.36.0
# Terraform Registry: https://registry.terraform.io/providers/hashicorp/aws/6.36.0/docs/resources/cloudfront_anycast_ip_list
# Generated: 2026-03-18
#
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       最新の公式ドキュメントを必ず確認してください。

#-----------------------------------------------------------------------
# 基本設定
#-----------------------------------------------------------------------

resource "aws_cloudfront_anycast_ip_list" "example" {
  # 設定内容: Anycast静的IPリストの名前
  # 必須: はい（変更時は再作成）
  # 用途: CloudFrontコンソールやAPIでこのIPリストを識別するための名前です。
  #       ディストリビューションへの関連付け時にこの名前で参照します。
  name = "example-anycast-ip-list"

  # 設定内容: 割り当てる静的IPアドレスの数
  # 設定可能な値: 3, 21
  # 必須: はい（変更時は再作成）
  # 用途: Anycast静的IPリストに割り当てるIPアドレスの数を指定します。
  #       通常のユースケースでは21を、apexドメイン用途では3を指定します。
  ip_count = 21

  #-----------------------------------------------------------------------
  # タグ設定
  #-----------------------------------------------------------------------

  # 設定内容: リソースに付与するタグ
  # 省略時: タグなし（provider default_tags設定がある場合はそちらが適用）
  # 用途: コスト管理、リソースの分類・検索、アクセス制御に使用します。
  tags = {
    Name        = "example-anycast-ip-list"
    Environment = "production"
    ManagedBy   = "terraform"
  }

  #-----------------------------------------------------------------------
  # タイムアウト設定
  #-----------------------------------------------------------------------

  # timeouts {
  #   # 設定内容: リソース作成時のタイムアウト時間
  #   # 形式: "30s", "5m", "1h"などの期間文字列
  #   # 省略時: デフォルトのタイムアウト時間が適用されます
  #   create = "30m"
  # }
}

#-----------------------------------------------------------------------
# Attributes Reference
#-----------------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id        : Anycast静的IPリストのID
# - arn       : Anycast静的IPリストのARN
# - etag      : リソースの現在のバージョン識別子（更新時の競合検出に使用）
# - anycast_ips: 割り当てられた静的IPアドレスのリスト
# - tags_all  : provider default_tagsとマージされた全タグのマップ
#-----------------------------------------------------------------------
