#---------------------------------------------------------------
# Amazon EC2 スナップショット ボリューム作成権限
#---------------------------------------------------------------
#
# 指定したAWSアカウントに対して、EBSスナップショットからボリュームを
# 作成する権限を付与するリソースです。
# スナップショットを他のAWSアカウントと共有する際に使用します。
#
# AWS公式ドキュメント:
#   - スナップショットの共有: https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-modifying-snapshot-permissions.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/snapshot_create_volume_permission
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_snapshot_create_volume_permission" "example" {
  #-------------------------------------------------------------
  # 権限設定
  #-------------------------------------------------------------

  # snapshot_id (Required)
  # 設定内容: ボリューム作成権限を付与する対象のスナップショットIDを指定します。
  # 設定可能な値: 有効なスナップショットID（例: snap-0123456789abcdef0）
  snapshot_id = "snap-0123456789abcdef0"

  # account_id (Required)
  # 設定内容: スナップショットのボリューム作成権限を付与するAWSアカウントIDを指定します。
  # 設定可能な値: 12桁の数字で構成されるAWSアカウントID
  account_id = "123456789012"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {
    # create (Optional)
    # 設定内容: 権限付与操作のタイムアウト時間を指定します。
    # 設定可能な値: "60s", "10m", "2h" 等の時間文字列
    create = null

    # delete (Optional)
    # 設定内容: 権限削除操作のタイムアウト時間を指定します。
    # 設定可能な値: "60s", "10m", "2h" 等の時間文字列
    delete = null
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: スナップショットID と アカウントID をハイフンで結合した文字列
#---------------------------------------------------------------
