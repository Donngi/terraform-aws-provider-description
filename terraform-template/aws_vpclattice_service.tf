#---------------------------------------------------------------
# AWS VPC Lattice Service
#---------------------------------------------------------------
#
# VPC Latticeサービスを管理するリソース。
# サービスはVPC Latticeサービスネットワーク内のマイクロサービスを表し、
# ルーティング設定、認証、カスタムドメインを構成できる。
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/vpclattice_service
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
# Attributes Reference (参照可能な属性):
#   id               - サービスID
#   arn              - サービスのARN
#   status           - サービスのステータス
#   dns_entry        - DNSエントリのリスト（domain_name, hosted_zone_id）
#   tags_all         - すべてのタグ（プロバイダーのdefault_tagsを含む）
#
#---------------------------------------------------------------

resource "aws_vpclattice_service" "example" {

  #---------------------------------------
  # 基本設定
  #---------------------------------------

  # 設定内容: サービス名
  # 設定可能な値: 3〜40文字の英数字、ハイフン
  # 省略時: 設定必須
  name = "example-service"

  #---------------------------------------
  # 認証・セキュリティ設定
  #---------------------------------------

  # 設定内容: サービスの認証タイプ
  # 設定可能な値: "NONE" | "AWS_IAM"
  # 省略時: "NONE"
  auth_type = "NONE"

  # 設定内容: カスタムドメインに使用するACM証明書のARN
  # 設定可能な値: 有効なACM証明書ARN（custom_domain_nameと併用）
  # 省略時: 証明書なし
  certificate_arn = null

  #---------------------------------------
  # ドメイン設定
  #---------------------------------------

  # 設定内容: サービスのカスタムドメイン名
  # 設定可能な値: 有効なFQDN
  # 省略時: カスタムドメインなし
  custom_domain_name = null

  #---------------------------------------
  # リージョン設定
  #---------------------------------------

  # 設定内容: リソースを管理するAWSリージョン
  # 設定可能な値: 有効なAWSリージョンコード
  # 省略時: プロバイダー設定のリージョン
  region = null

  #---------------------------------------
  # タグ設定
  #---------------------------------------

  # 設定内容: リソースに付与するタグ
  # 省略時: タグなし
  tags = {
    Name = "example-service"
  }

  #---------------------------------------
  # タイムアウト設定
  #---------------------------------------

  timeouts {
    # 設定内容: 作成タイムアウト
    # 省略時: デフォルトタイムアウト
    create = "5m"

    # 設定内容: 更新タイムアウト
    # 省略時: デフォルトタイムアウト
    update = "5m"

    # 設定内容: 削除タイムアウト
    # 省略時: デフォルトタイムアウト
    delete = "5m"
  }
}
