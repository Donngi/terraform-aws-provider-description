#---------------------------------------------------------------
# AWS MemoryDB Subnet Group
#---------------------------------------------------------------
#
# Amazon MemoryDB クラスターをVPC環境で運用するためのサブネットグループを
# プロビジョニングするリソースです。サブネットグループはプライベートサブネットの
# コレクションで、MemoryDBクラスターのノードに割り当てるサブネットとIPアドレスを
# 決定するために使用されます。
#
# AWS公式ドキュメント:
#   - サブネットとサブネットグループ: https://docs.aws.amazon.com/memorydb/latest/devguide/subnetgroups.html
#   - サブネットグループの作成: https://docs.aws.amazon.com/memorydb/latest/devguide/subnetgroups.creating.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/memorydb_subnet_group
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_memorydb_subnet_group" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # subnet_ids (Required)
  # 設定内容: サブネットグループに関連付けるVPCサブネットIDのセットを指定します。
  # 設定可能な値: 有効なサブネットIDの集合。最低1つのサブネットが必要。
  # 注意: クラスターが高可用性を確保するために、複数のアベイラビリティーゾーンに
  #       わたるサブネットを指定することを推奨します。
  # 参考: https://docs.aws.amazon.com/memorydb/latest/devguide/subnetgroups.creating.html
  subnet_ids = ["subnet-12345678", "subnet-87654321"]

  #-------------------------------------------------------------
  # 名前設定
  #-------------------------------------------------------------

  # name (Optional, Forces new resource)
  # 設定内容: サブネットグループの名前を指定します。
  # 設定可能な値: 文字列。他のサブネットグループと重複しない一意の名前。
  # 省略時: Terraformがランダムな一意の名前を生成します。
  # 注意: name_prefix と排他的（どちらか一方のみ指定可能）
  name = "my-subnet-group"

  # name_prefix (Optional, Forces new resource)
  # 設定内容: サブネットグループ名のプレフィックスを指定します。Terraformが後ろにランダムなサフィックスを追加します。
  # 設定可能な値: 文字列
  # 省略時: Terraformがランダムな一意の名前を生成します。
  # 注意: name と排他的（どちらか一方のみ指定可能）
  name_prefix = null

  #-------------------------------------------------------------
  # 説明設定
  #-------------------------------------------------------------

  # description (Optional)
  # 設定内容: サブネットグループの説明を指定します。
  # 設定可能な値: 文字列
  # 省略時: "Managed by Terraform" が設定されます。
  description = "My MemoryDB subnet group"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用します。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  # 注意: プロバイダーレベルの default_tags 設定ブロックで定義されたタグと
  #       一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "my-subnet-group"
    Environment = "production"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: サブネットグループの名前
# - arn: サブネットグループのARN
# - vpc_id: サブネットグループが存在するVPCのID
# - tags_all: プロバイダーの default_tags 設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
