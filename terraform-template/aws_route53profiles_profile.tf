#---------------------------------------------------------------
# AWS Route 53 Profile
#---------------------------------------------------------------
#
# Amazon Route 53 ProfileをプロビジョニングするTerraformリソースです。
# Route 53 Profilesは、複数のVPCやAWSアカウントにわたってDNS関連の設定を
# 一元管理・適用するための機能です。プライベートホストゾーン、Resolverルール、
# DNS Firewallルールグループ、VPC Resolverクエリログ設定、インターフェイスVPC
# エンドポイントなどのDNSリソースをProfileに関連付け、複数のVPCに一括適用
# できます。
#
# AWS公式ドキュメント:
#   - Amazon Route 53 Profiles概要: https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/profiles.html
#   - Route 53 Profile作成方法: https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/profile-create.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/route53profiles_profile
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_route53profiles_profile" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # name (Required)
  # 設定内容: Route 53 Profileの名前を指定します。
  # 設定可能な値: 文字列
  name = "example-profile"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するAWSリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用します。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  # 注意: プロバイダーのdefault_tags設定ブロックで定義されたタグと
  #       一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "example-profile"
    Environment = "production"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成操作のタイムアウト時間を指定します。
    # 設定可能な値: 時間単位のサフィックスを含む文字列（例: "30s", "5m", "2h45m"）
    #   使用可能な単位: "s"（秒）, "m"（分）, "h"（時間）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    create = "30m"

    # delete (Optional)
    # 設定内容: リソース削除操作のタイムアウト時間を指定します。
    # 設定可能な値: 時間単位のサフィックスを含む文字列（例: "30s", "5m", "2h45m"）
    #   使用可能な単位: "s"（秒）, "m"（分）, "h"（時間）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    # 注意: 削除操作のタイムアウトは、destroyが実行される前にstateに保存されている
    #       変更がある場合にのみ適用されます。
    delete = "30m"

    # read (Optional)
    # 設定内容: リソース読み取り操作のタイムアウト時間を指定します。
    # 設定可能な値: 時間単位のサフィックスを含む文字列（例: "30s", "5m", "2h45m"）
    #   使用可能な単位: "s"（秒）, "m"（分）, "h"（時間）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    # 注意: 読み取り操作は、refreshが有効な場合の全てのrefreshまたはplanning操作中に発生します。
    read = "10m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: ProfileのAmazon Resource Name (ARN)
# - id: ProfileのID
# - owner_id: Profileを所有するAWSアカウントID
# - share_status: Profileの共有ステータス
# - status: Profileの現在のステータス
# - status_message: Profileのステータスに関するメッセージ
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
