#---------------------------------------------------------------
# AWS WAF Regional Rule Group
#---------------------------------------------------------------
#
# AWS WAF Regional ルールグループをプロビジョニングするリソースです。
# ルールグループは、複数のWAFルールをグループ化して管理するためのコンテナです。
# リージョナルルールグループは、Application Load Balancer (ALB) や
# API Gatewayなどのリージョナルリソースを保護するために使用されます。
#
# 注意: このリソースはAWS WAF Classicを使用しています。
#       新規の実装にはAWS WAFv2 (aws_wafv2_rule_group) の使用を推奨します。
#
# AWS公式ドキュメント:
#   - AWS WAF概要: https://docs.aws.amazon.com/waf/latest/developerguide/what-is-aws-waf.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/wafregional_rule_group
#
# Provider Version: 6.28.0
# Generated: 2026-02-06
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_wafregional_rule_group" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # name (Required)
  # 設定内容: ルールグループのフレンドリーな名前を指定します。
  # 設定可能な値: 文字列
  # 注意: ルールグループを識別するための一意の名前を指定してください。
  name = "example-rule-group"

  # metric_name (Required)
  # 設定内容: ルールグループのメトリクス用のフレンドリーな名前を指定します。
  # 設定可能な値: 英数字のみの文字列（特殊文字不可）
  # 注意: CloudWatchメトリクスで使用される名前です。
  #       スペースやハイフンなどの特殊文字は使用できません。
  metric_name = "exampleRuleGroup"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # ルール設定
  #-------------------------------------------------------------

  # activated_rule (Optional)
  # 設定内容: ルールグループに追加するアクティブ化されたルールのリストを指定します。
  # 注意: 各ルールには優先度、ルールID、アクションを指定する必要があります。
  #       AWS WAF Classicルールグループには最大10個のルールを追加できます。
  activated_rule {
    # priority (Required)
    # 設定内容: ルールの優先度を指定します。
    # 設定可能な値: 数値（小さい値ほど優先度が高い）
    # 注意: ルールグループ内で一意である必要があります。
    #       WAFは優先度の低い（数値が小さい）ルールから評価します。
    priority = 1

    # rule_id (Required)
    # 設定内容: ルールグループに追加するWAFルールのIDを指定します。
    # 設定可能な値: 有効なaws_wafregional_ruleリソースのID
    # 参考: aws_wafregional_ruleリソースで作成したルールのIDを指定します。
    rule_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

    # type (Optional)
    # 設定内容: ルールのタイプを指定します。
    # 設定可能な値:
    #   - "REGULAR": 通常のルール（デフォルト）
    #   - "RATE_BASED": レートベースのルール
    #   - "GROUP": ルールグループ
    # 省略時: "REGULAR"
    type = "REGULAR"

    # action (Required)
    # 設定内容: ルールに一致したリクエストに対するアクションを指定します。
    action {
      # type (Required)
      # 設定内容: アクションのタイプを指定します。
      # 設定可能な値:
      #   - "ALLOW": リクエストを許可
      #   - "BLOCK": リクエストをブロック
      #   - "COUNT": リクエストをカウント（許可/ブロックせず統計のみ取得）
      # 注意: COUNTアクションはルールのテストやモニタリングに有用です。
      type = "COUNT"
    }
  }

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "example-rule-group"
    Environment = "production"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: WAF Regional ルールグループのID
#
# - arn: WAF Regional ルールグループのAmazon Resource Name (ARN)
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ。
#---------------------------------------------------------------
