#---------------------------------------------------------------
# AWS Service Quotas Template Association
#---------------------------------------------------------------
#
# AWS Service Quotasのクォータ増加テンプレートを組織に関連付けるリソースです。
# テンプレートを組織に関連付けることで、組織に新しく追加されたAWSアカウントに対して
# テンプレートで定義されたクォータ増加リクエストが自動的に適用されます。
#
# 注意: このリソースは組織の管理アカウントからのみ操作可能であり、
#       us-east-1リージョンでのみ実行する必要があります。
#
# AWS公式ドキュメント:
#   - Service Quotas テンプレート概要: https://docs.aws.amazon.com/servicequotas/latest/userguide/organization-templates.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/servicequotas_template_association
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_servicequotas_template_association" "example" {
  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional, Computed)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: AWSリージョンコード（例: "us-east-1"）
  # 省略時: プロバイダー設定のリージョンが使用されます。
  # 注意: Service Quotasテンプレートの関連付けはus-east-1リージョンから実行する必要があります。
  region = "us-east-1"

  #-------------------------------------------------------------
  # 削除時の動作設定
  #-------------------------------------------------------------

  # skip_destroy (Optional)
  # 設定内容: リソース削除時にテンプレートの関連付けを解除せずに
  #           Terraformの状態からのみ削除するかどうかを指定します。
  # 設定可能な値:
  #   - true: Terraform状態からのみ削除し、リモートの関連付けはそのまま維持します。
  #   - false: Terraform削除時にリモートの関連付けも解除します。
  # 省略時: false（リソース削除時に関連付けも解除されます）
  skip_destroy = false
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: AWSアカウントID
# - status: 関連付けのステータス。このリソースを作成すると "ASSOCIATED" となり、
#           テンプレートのクォータ増加リクエストが組織の新規アカウントに自動適用されます。
#---------------------------------------------------------------
