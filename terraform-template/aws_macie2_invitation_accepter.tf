#---------------------------------------------------------------
# Amazon Macie Invitation Accepter
#---------------------------------------------------------------
#
# Amazon Macie招待承認リソースです。
# Macieは、機密データを自動的に検出、分類、保護するフルマネージドサービスで、
# 管理者アカウントから送信されたMacie招待を承認する際に使用します。
# メンバーアカウント側でこのリソースを作成することで、管理者アカウントとの
# 関係を確立し、Macieの組織管理に参加できます。
#
# AWS公式ドキュメント:
#   - Amazon Macie概要: https://docs.aws.amazon.com/macie/latest/user/what-is-macie.html
#   - メンバーアカウント管理: https://docs.aws.amazon.com/macie/latest/user/macie-accounts.html
#   - Macie API Reference (Accept Invitation): https://docs.aws.amazon.com/macie/latest/APIReference/invitations-accept.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/macie2_invitation_accepter
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_macie2_invitation_accepter" "example" {
  #-------------------------------------------------------------
  # 管理者アカウント設定 (必須)
  #-------------------------------------------------------------

  # administrator_account_id (Required)
  # 設定内容: 招待を送信した管理者アカウントのAWSアカウントIDを指定します。
  # 設定可能な値: 12桁のAWSアカウントID
  # 注意: このアカウントIDはMacieで管理者として設定されたアカウントのIDです。
  #       管理者アカウントは事前にaws_macie2_memberリソースで招待を送信している必要があります。
  #       メンバーアカウント側のプロバイダーでこのリソースを作成してください。
  administrator_account_id = "123456789012"

  #-------------------------------------------------------------
  # リージョン設定 (オプション)
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: us-east-1, ap-northeast-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 注意: Macieはリージョナルサービスです。管理者アカウントとメンバーアカウントは
  #       同じリージョンでMacieを有効化する必要があります。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タイムアウト設定 (オプション)
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソースの作成時のタイムアウト時間を指定します。
  # 注意: 招待の承認処理に時間がかかる場合に、デフォルトのタイムアウト値を上書きできます。
  timeouts {
    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間
    # 設定可能な値: Go言語のtime.Duration形式（例: "60m", "1h"）
    # 省略時: デフォルトのタイムアウト値が使用されます
    create = "60m"
  }

  #-------------------------------------------------------------
  # 依存関係設定
  #-------------------------------------------------------------

  # 注意: このリソースを使用する前に、管理者アカウントからの招待が送信されている必要があります。
  # 管理者アカウント側のaws_macie2_memberリソースへの依存関係を明示的に設定することを推奨します。
  # 例:
  # depends_on = [aws_macie2_member.primary]
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: Macie招待承認の一意識別子（招待ID）
#
# - invitation_id: 承認した招待の一意識別子
#
#---------------------------------------------------------------
