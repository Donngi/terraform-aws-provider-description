#---------------------------------------------------------------
# Amazon Location Service Route Calculator
#---------------------------------------------------------------
#
# Amazon Location Serviceのルートカルキュレーターリソースをプロビジョニングするリソースです。
# ルートカルキュレーターは、出発地と目的地の間のルートを計算し、移動時間や距離を
# 推定するために使用されます。最大23のウェイポイントを含む経路計算や、
# 複数の出発地・目的地間のルートマトリクス計算をサポートします。
#
# AWS公式ドキュメント:
#   - Amazon Location Serviceルーティング概要: https://docs.aws.amazon.com/location/previous/developerguide/route-concepts.html
#   - ルートの計算: https://docs.aws.amazon.com/location/previous/developerguide/calculating-routes.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/location_route_calculator
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_location_route_calculator" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # calculator_name (Required)
  # 設定内容: ルートカルキュレーターリソースの名前を指定します。
  # 設定可能な値: 1-100文字の英数字、ハイフン、アンダースコア、ピリオドを含む文字列
  calculator_name = "example"

  # data_source (Required)
  # 設定内容: 交通情報および道路ネットワークデータのプロバイダーを指定します。
  # 設定可能な値:
  #   - "Esri": Esriが提供する地図・ルーティングデータ
  #   - "Grab": Grab（東南アジア向け）が提供するデータ
  #   - "Here": HEREが提供する交通・道路ネットワークデータ
  # 注意: データソースはリソース作成後に変更できません（再作成が必要）。
  #       Grabデータソースはap-southeast-1リージョンでのみ利用可能です。
  data_source = "Here"

  # description (Optional)
  # 設定内容: ルートカルキュレーターリソースの説明を指定します。
  # 設定可能な値: 0-1000文字の文字列
  # 省略時: 説明なし
  description = null

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するAWSリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "example"
    Environment = "production"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  timeouts {
    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "30m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    create = null

    # update (Optional)
    # 設定内容: リソース更新時のタイムアウト時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "30m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    update = null

    # delete (Optional)
    # 設定内容: リソース削除時のタイムアウト時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "30m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    delete = null
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - calculator_arn: ルートカルキュレーターリソースのAmazon Resource Name (ARN)。
#                  AWS全体でリソースを指定する際に使用します。
#
# - create_time: ルートカルキュレーターリソースが作成された日時（ISO 8601形式）。
#
# - update_time: ルートカルキュレーターリソースが最後に更新された日時（ISO 8601形式）。
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ。
#---------------------------------------------------------------
