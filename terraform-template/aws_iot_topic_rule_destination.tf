#---------------------------------------------------------------
# AWS IoT トピックルール送信先
#---------------------------------------------------------------
#
# AWS IoT Core のトピックルール送信先（Topic Rule Destination）を
# プロビジョニングするリソースです。
# トピックルール送信先は、IoT ルールエンジンが HTTP エンドポイントや
# VPC 内のリソースにメッセージを転送するための設定を定義します。
# VPC 送信先を使用することで、パブリックインターネットを経由せずに
# VPC 内のプライベートリソース（ALB、NLB 等）へのルーティングが可能です。
#
# AWS公式ドキュメント:
#   - トピックルール送信先: https://docs.aws.amazon.com/iot/latest/developerguide/rule-destination.html
#   - VPC 送信先の設定: https://docs.aws.amazon.com/iot/latest/developerguide/vpc-rule-action.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/iot_topic_rule_destination
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_iot_topic_rule_destination" "example" {
  #-------------------------------------------------------------
  # 有効化設定
  #-------------------------------------------------------------

  # enabled (Optional)
  # 設定内容: トピックルール送信先を有効化するかを指定します。
  # 設定可能な値:
  #   - true: 送信先を有効化します（デフォルト）
  #   - false: 送信先を無効化します
  # 注意: 送信先が有効化されていない場合、ルールエンジンはこの送信先へのルーティングを行いません。
  enabled = true

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理する AWS リージョンを指定します。
  # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用します。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # VPC 設定
  #-------------------------------------------------------------

  # vpc_configuration (Required)
  # 設定内容: VPC 内のリソースへルーティングするための VPC 送信先設定を指定します。
  # 注意: このブロックは必須であり、1 件のみ指定可能です。
  vpc_configuration {
    # vpc_id (Required)
    # 設定内容: 送信先として使用する VPC の ID を指定します。
    # 設定可能な値: 有効な VPC ID（例: vpc-xxxxxxxx）
    vpc_id = "vpc-xxxxxxxxxxxxxxxx"

    # subnet_ids (Required)
    # 設定内容: VPC 内のサブネット ID のセットを指定します。
    # 設定可能な値: 有効なサブネット ID のセット
    # 注意: AWS IoT はこれらのサブネット内にネットワークインターフェースを作成します。
    #       プライベートサブネットを使用する場合、NAT ゲートウェイが不要で
    #       VPC エンドポイント経由でアクセス可能です。
    subnet_ids = [
      "subnet-xxxxxxxxxxxxxxxx",
      "subnet-yyyyyyyyyyyyyyyy",
    ]

    # security_groups (Optional)
    # 設定内容: VPC 内のネットワークインターフェースに適用するセキュリティグループ ID のセットを指定します。
    # 設定可能な値: 有効なセキュリティグループ ID のセット
    # 省略時: デフォルトのセキュリティグループが適用されます。
    security_groups = [
      "sg-xxxxxxxxxxxxxxxx",
    ]

    # role_arn (Required)
    # 設定内容: AWS IoT が VPC 内のリソースにアクセスするために引き受ける IAM ロールの ARN を指定します。
    # 設定可能な値: 有効な IAM ロールの ARN
    # 注意: このロールには、VPC 内のネットワークインターフェースの作成・削除に関する
    #       EC2 アクションを許可するポリシーがアタッチされている必要があります。
    role_arn = "arn:aws:iam::123456789012:role/iot-topic-rule-destination-role"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソースの作成・更新・削除操作のタイムアウト時間を指定します。
  timeouts {
    # create (Optional)
    # 設定内容: リソース作成のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" などの時間文字列
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    create = "30m"

    # update (Optional)
    # 設定内容: リソース更新のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" などの時間文字列
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    update = "30m"

    # delete (Optional)
    # 設定内容: リソース削除のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" などの時間文字列
    # 省略時: プロバイダーのデフォルトタイムアウトを使用します。
    delete = "30m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: トピックルール送信先の Amazon Resource Name (ARN)
#
# - id: トピックルール送信先の識別子（ARN と同じ値）
#---------------------------------------------------------------
