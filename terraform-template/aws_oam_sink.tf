#---------------------------------------------------------------
# Amazon CloudWatch Observability Access Manager (OAM) Sink
#---------------------------------------------------------------
#
# CloudWatch Observability Access Manager (OAM) の Sink（シンク）を
# プロビジョニングするリソースです。Sink はモニタリングデータを受け取る側の
# アカウント（モニタリングアカウント）に作成されるリソースで、
# ソースアカウントから共有されたメトリクス・ログ・トレースなどの
# オブザーバビリティデータを集約します。
#
# Sink を作成した後は、aws_oam_sink_policy リソースを使用して
# データを共有するソースアカウントを許可するポリシーをアタッチします。
#
# AWS公式ドキュメント:
#   - OAM の概要: https://docs.aws.amazon.com/OAM/latest/APIReference/Welcome.html
#   - CloudWatch クロスアカウントオブザーバビリティ: https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Unified-Cross-Account.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/oam_sink
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_oam_sink" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # name (Required)
  # 設定内容: Sink の名前を指定します。
  # 設定可能な値: 1〜255文字の文字列（英数字、ハイフン、アンダースコア）
  # 省略時: 省略不可
  name = "example-oam-sink"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  tags = {
    Name        = "example-oam-sink"
    Environment = "production"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を指定します。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" のような時間文字列
    # 省略時: プロバイダーのデフォルト値を使用
    create = "30m"

    # update (Optional)
    # 設定内容: リソース更新時のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" のような時間文字列
    # 省略時: プロバイダーのデフォルト値を使用
    update = "30m"

    # delete (Optional)
    # 設定内容: リソース削除時のタイムアウト時間を指定します。
    # 設定可能な値: "30m" や "1h" のような時間文字列
    # 省略時: プロバイダーのデフォルト値を使用
    delete = "30m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: Sink の Amazon Resource Name (ARN)
# - id: Sink の ARN（arn と同じ値）
# - sink_id: Sink を一意に識別するランダムな ID 文字列
# - tags_all: プロバイダーの default_tags 設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
