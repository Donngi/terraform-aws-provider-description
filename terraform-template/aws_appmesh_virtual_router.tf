#---------------------------------------------------------------
# AWS App Mesh Virtual Router
#---------------------------------------------------------------
#
# AWS App Meshのバーチャルルーターをプロビジョニングするリソースです。
# バーチャルルーターはメッシュ内の1つ以上のバーチャルサービスのトラフィックを
# 処理し、ルートに基づいてリクエストを異なるバーチャルノードに転送します。
#
# AWS公式ドキュメント:
#   - App Mesh概要: https://docs.aws.amazon.com/app-mesh/latest/userguide/what-is-app-mesh.html
#   - Virtual Routers: https://docs.aws.amazon.com/app-mesh/latest/userguide/virtual_routers.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/appmesh_virtual_router
#
# Provider Version: 6.28.0
# Generated: 2026-01-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_appmesh_virtual_router" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # name (Required, Forces new resource)
  # 設定内容: バーチャルルーターの名前を指定します。
  # 設定可能な値: 1-255文字の文字列（英数字、ハイフン、アンダースコア）
  name = "my-virtual-router"

  # mesh_name (Required, Forces new resource)
  # 設定内容: バーチャルルーターを作成するサービスメッシュの名前を指定します。
  # 設定可能な値: 1-255文字の文字列（既存のメッシュ名）
  mesh_name = "my-app-mesh"

  # mesh_owner (Optional)
  # 設定内容: サービスメッシュの所有者のAWSアカウントIDを指定します。
  # 設定可能な値: 12桁のAWSアカウントID
  # 省略時: AWSプロバイダーが現在接続しているアカウントIDを使用
  # 用途: クロスアカウントでメッシュを共有する場合に使用
  mesh_owner = null

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # 仕様設定 (spec)
  #-------------------------------------------------------------

  # spec (Required)
  # 設定内容: バーチャルルーターの仕様を定義します。
  spec {
    #-----------------------------------------------------------
    # リスナー設定
    #-----------------------------------------------------------

    # listener (Optional)
    # 設定内容: バーチャルルーターがインバウンドトラフィックを受信する
    #          リスナーを定義します。
    # 注意: 現在、バーチャルルーターごとに1つのリスナーのみサポート
    listener {
      # port_mapping (Required)
      # 設定内容: リスナーのポートマッピング情報を指定します。
      port_mapping {
        # port (Required)
        # 設定内容: ポートマッピングに使用するポート番号を指定します。
        # 設定可能な値: 1-65535の整数
        port = 8080

        # protocol (Required)
        # 設定内容: ポートマッピングに使用するプロトコルを指定します。
        # 設定可能な値:
        #   - "http": HTTP/1.1プロトコル（WebSocket接続への移行をサポート）
        #   - "http2": HTTP/2プロトコル
        #   - "tcp": TCPプロトコル
        #   - "grpc": gRPCプロトコル
        protocol = "http"
      }
    }
  }

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 関連機能: AWSリソースタグ付け
  #   プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #   一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "my-virtual-router"
    Environment = "production"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: バーチャルルーターのID
#
# - arn: バーチャルルーターのAmazon Resource Name (ARN)
#
# - created_date: バーチャルルーターの作成日時
#
# - last_updated_date: バーチャルルーターの最終更新日時
#
# - resource_owner: リソース所有者のAWSアカウントID
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
