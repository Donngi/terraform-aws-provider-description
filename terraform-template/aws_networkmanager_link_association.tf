#---------------------------------------------------------------
# AWS Network Manager Link Association
#---------------------------------------------------------------
#
# グローバルネットワーク内のデバイスとリンクを関連付けるリソースです。
# リンクアソシエーションにより、デバイスがどのリンクを使用するかを定義します。
# 1つのリンクに対して複数のデバイスを関連付けることができます。
#
# AWS公式ドキュメント:
#   - Network Manager リンクアソシエーション: https://docs.aws.amazon.com/network-manager/latest/tgwnm/device-links.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/networkmanager_link_association
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_networkmanager_link_association" "example" {
  #-------------------------------------------------------------
  # グローバルネットワーク設定
  #-------------------------------------------------------------

  # global_network_id (Required)
  # 設定内容: リンクアソシエーションが所属するグローバルネットワークのIDを指定します。
  # 設定可能な値: 有効なグローバルネットワークID文字列
  global_network_id = "network-1234567890abcdef0"

  #-------------------------------------------------------------
  # デバイスおよびリンク設定
  #-------------------------------------------------------------

  # device_id (Required)
  # 設定内容: 関連付けるデバイスのIDを指定します。
  # 設定可能な値: 有効なデバイスID文字列
  device_id = "device-1234567890abcdef0"

  # link_id (Required)
  # 設定内容: 関連付けるリンクのIDを指定します。
  # 設定可能な値: 有効なリンクID文字列
  link_id = "link-1234567890abcdef0"

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  timeouts {
    # create (Optional)
    # 設定内容: リソースの作成完了を待機する最大時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "30m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトが使用されます。
    create = "30m"

    # delete (Optional)
    # 設定内容: リソースの削除完了を待機する最大時間を指定します。
    # 設定可能な値: Goのtime.Duration形式の文字列（例: "30m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトが使用されます。
    delete = "30m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: グローバルネットワークID、リンクID、デバイスIDを組み合わせた一意の識別子
#---------------------------------------------------------------
