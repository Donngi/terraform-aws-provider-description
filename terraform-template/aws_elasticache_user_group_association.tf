#-----------------------------------------------------------------------
# Provider Version: 6.28.0
# Resource: aws_elasticache_user_group_association
# Generated: 2026-02-17
# Terraform Registry: https://registry.terraform.io/providers/hashicorp/aws/6.28.0/docs/resources/elasticache_user_group_association
#
# NOTE: このテンプレートはAWS Provider v6.28.0のスキーマから自動生成されています。
#       実際の利用時は必要な属性のみを使用してください。
#-----------------------------------------------------------------------
# 概要:
# ElastiCacheユーザーグループにユーザーを関連付けるリソース。
# Redis用のロールベースアクセス制御（RBAC）の一部として、ユーザーを
# ユーザーグループに追加・削除する操作を管理します。
#
# ユースケース:
# - Redisユーザーグループへのユーザー追加
# - ロールベースアクセス制御（RBAC）の実装
# - ユーザー権限のグループ単位管理
# - 動的なユーザーグループメンバーシップ管理
#
# 制限事項:
# - Redis専用機能（MemcachedおよびValkeyでは使用不可）
# - user_group_idとuser_idの変更はリソース再作成が必要
# - ユーザーグループとユーザーは事前に作成されている必要がある
# - 1つのユーザーは複数のユーザーグループに所属可能
#
# 依存関係:
# - aws_elasticache_user_group（必須）
# - aws_elasticache_user（必須）
#
# 注意事項:
# - ユーザーグループやユーザーの削除前に関連付けを解除する必要がある
# - 関連付け操作は非同期で実行されるため完了に時間がかかる場合がある
# - 同一ユーザーの重複した関連付けはエラーとなる
#
#-----------------------------------------------------------------------

resource "aws_elasticache_user_group_association" "example" {
  #-----------------------------------------------------------------------
  # 基本設定
  #-----------------------------------------------------------------------

  # 設定内容: ユーザーグループ識別子
  # 設定可能な値: 既存のElastiCacheユーザーグループID
  # 省略時: 指定必須
  user_group_id = "my-user-group"

  # 設定内容: ユーザー識別子
  # 設定可能な値: 既存のElastiCacheユーザーID
  # 省略時: 指定必須
  user_id = "my-user"

  #-----------------------------------------------------------------------
  # リージョン設定
  #-----------------------------------------------------------------------

  # 設定内容: リソースが管理されるリージョン
  # 設定可能な値: ap-northeast-1, us-east-1等のリージョンコード
  # 省略時: プロバイダー設定のリージョン使用
  region = "ap-northeast-1"

  #-----------------------------------------------------------------------
  # タイムアウト設定
  #-----------------------------------------------------------------------

  timeouts {
    # 設定内容: リソース作成のタイムアウト時間
    # 設定可能な値: 時間文字列（例: 5m, 10m）
    # 省略時: デフォルトタイムアウト使用
    create = "5m"

    # 設定内容: リソース削除のタイムアウト時間
    # 設定可能な値: 時間文字列（例: 5m, 10m）
    # 省略時: デフォルトタイムアウト使用
    delete = "5m"
  }
}

#-----------------------------------------------------------------------
# Attributes Reference（参照可能な属性）
#-----------------------------------------------------------------------
# - id: 関連付け識別子（user_group_id,user_id形式）
# - user_group_id: ユーザーグループID
# - user_id: ユーザーID
# - region: リソースが管理されているリージョン
#-----------------------------------------------------------------------
