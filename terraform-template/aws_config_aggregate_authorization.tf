#-------------------------------------------------------------------------------------------------------
# AWS Config Aggregate Authorization
#-------------------------------------------------------------------------------------------------------
# 用途: AWS Configで集約設定データの収集元となるアカウントに対する認可を付与するリソース
# - マルチアカウント環境でのConfig集約を実現
# - 別のアカウントに配置されたConfig Aggregatorへのデータ提供を許可
# - アカウントIDとリージョンの組み合わせで認可を管理
# - 削除時は集約データの収集が停止される
#
# 関連リソース:
# - aws_config_configuration_aggregator: この認可を使用してデータを集約するAggregator
#
# Provider Version: 6.28.0
# Generated: 2026-02-13
# NOTE: このテンプレートは自動生成されています。実際の環境に合わせてパラメータを調整してください。
#
# 公式ドキュメント: https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/config_aggregate_authorization
#-------------------------------------------------------------------------------------------------------

resource "aws_config_aggregate_authorization" "example" {

  #-------------------------------------------------------------------------------------------------------
  # 認可対象の設定
  #-------------------------------------------------------------------------------------------------------

  # 設定内容: Config Aggregatorを実行するAWSアカウントのID
  # - 12桁の数字で構成
  # - このアカウントIDのAggregatorにデータ提供を許可
  # - マルチアカウント戦略において中央管理用アカウントを指定することが多い
  account_id = "123456789012"

  #-------------------------------------------------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------------------------------------------------

  # 設定内容: 認可するAWSリージョン（推奨パラメータ）
  # 設定可能な値: ap-northeast-1、us-east-1、eu-west-1等の有効なAWSリージョン名
  # 省略時: 現在のプロバイダーリージョンが使用される
  # - regionパラメータよりも優先される
  # - 新規作成時はこちらを使用すること
  authorized_aws_region = "ap-northeast-1"

  # 設定内容: 認可するAWSリージョン（非推奨パラメータ）
  # 設定可能な値: ap-northeast-1、us-east-1、eu-west-1等の有効なAWSリージョン名
  # 省略時: 現在のプロバイダーリージョンが使用される
  # - authorized_aws_regionの使用を推奨（こちらは非推奨）
  # - 後方互換性のために残されている
  region = "ap-northeast-1"

  #-------------------------------------------------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------------------------------------------------

  # 設定内容: リソースに付与するタグのマップ
  # - コスト配分、リソース管理、アクセス制御に活用
  # - 組織の命名規則に従ったタグを設定
  tags = {
    Name        = "config-aggregate-authorization-example"
    Environment = "production"
    ManagedBy   = "Terraform"
  }
}

#-------------------------------------------------------------------------------------------------------
# Attributes Reference（参照可能な属性）
#-------------------------------------------------------------------------------------------------------
# このリソースから参照できる属性:
#
# - id
#   - 認可リソースの一意の識別子
#   - フォーマット: {account_id}:{region}
#
# - arn
#   - 認可リソースのARN
#   - フォーマット: arn:aws:config:{region}:{account_id}:aggregate-authorization/{account_id}/{region}
#   - IAMポリシーでのリソース指定に使用可能
#
# - tags_all
#   - プロバイダーのdefault_tagsとリソース固有のtagsを統合したマップ
#   - プロバイダーレベルで設定された共通タグも含まれる
#-------------------------------------------------------------------------------------------------------
