#---------------------------------------------------------------
# Amazon Lex V2 Bot Locale
#---------------------------------------------------------------
#
# Amazon Lex V2 のボットロケールをプロビジョニングするリソースです。
# ボットロケールは特定の言語・地域向けの会話モデルを定義し、
# NLU（自然言語理解）の信頼スコアしきい値や音声設定を管理します。
# ロケールごとにインテントやスロットタイプを独立して設定できます。
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/lexv2models_bot_locale
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_lexv2models_bot_locale" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # bot_id (Required)
  # 設定内容: ロケールを追加するボットの ID を指定します。
  # 設定可能な値: 既存の aws_lexv2models_bot リソースの bot_id
  # 省略時: 省略不可
  bot_id = aws_lexv2models_bot.example.id

  # bot_version (Required)
  # 設定内容: ロケールを追加するボットのバージョンを指定します。
  # 設定可能な値:
  #   - "DRAFT": ドラフトバージョン（開発中）
  #   - 特定のバージョン番号（例: "1", "2"）
  # 省略時: 省略不可
  bot_version = "DRAFT"

  # locale_id (Required)
  # 設定内容: ボットに追加するロケールの識別子を指定します。
  # 設定可能な値:
  #   - "de-AT": ドイツ語（オーストリア）
  #   - "de-DE": ドイツ語（ドイツ）
  #   - "en-AU": 英語（オーストラリア）
  #   - "en-GB": 英語（イギリス）
  #   - "en-IN": 英語（インド）
  #   - "en-US": 英語（米国）
  #   - "es-419": スペイン語（ラテンアメリカ）
  #   - "es-ES": スペイン語（スペイン）
  #   - "es-US": スペイン語（米国）
  #   - "fr-CA": フランス語（カナダ）
  #   - "fr-FR": フランス語（フランス）
  #   - "it-IT": イタリア語（イタリア）
  #   - "ja-JP": 日本語（日本）
  #   - "ko-KR": 韓国語（韓国）
  #   - "pt-BR": ポルトガル語（ブラジル）
  #   - "pt-PT": ポルトガル語（ポルトガル）
  #   - "zh-CN": 中国語（中国）
  # 省略時: 省略不可
  locale_id = "ja-JP"

  # n_lu_intent_confidence_threshold (Required)
  # 設定内容: NLU がインテントを識別するための信頼スコアのしきい値を指定します。
  # 設定可能な値: 0〜1 の数値（例: 0.4 = 40%の信頼スコア以上でインテントを一致とみなす）
  # 省略時: 省略不可
  # 注意: しきい値が低いほど多くのインテントが一致するが誤認識が増える。
  #       しきい値が高いほど精度は上がるが一致しないケースが増える。
  n_lu_intent_confidence_threshold = 0.4

  # name (Optional)
  # 設定内容: ロケールの表示名を指定します。
  # 設定可能な値: 文字列
  # 省略時: ロケール ID から自動生成
  name = null

  # description (Optional)
  # 設定内容: ロケールの説明を指定します。
  # 設定可能な値: 最大200文字の文字列
  # 省略時: 説明なし
  description = "日本語ロケール設定"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # 音声設定
  #-------------------------------------------------------------

  # voice_settings (Optional)
  # 設定内容: Amazon Polly を使用した音声インタラクションの設定を行うブロックです。
  # 省略時: 音声インタラクションなし
  voice_settings {

    # voice_id (Required)
    # 設定内容: Amazon Polly が音声インタラクションに使用する音声 ID を指定します。
    # 設定可能な値: Amazon Polly で有効な音声 ID（例: "Mizuki", "Takumi" など）
    # 省略時: 省略不可
    # 参考: https://docs.aws.amazon.com/polly/latest/dg/voicelist.html
    voice_id = "Mizuki"

    # engine (Optional)
    # 設定内容: Amazon Polly が使用する音声エンジンを指定します。
    # 設定可能な値:
    #   - "standard": 標準エンジン
    #   - "neural": ニューラルエンジン（より自然な音声）
    #   - "long-form": 長文向けエンジン
    # 省略時: ロケールとボイスに応じたデフォルトエンジン
    engine = null
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: Goの time.Duration 形式の文字列（例: "5m", "1h"）
    # 省略時: デフォルトタイムアウト
    create = "5m"

    # update (Optional)
    # 設定内容: リソース更新時のタイムアウト時間を指定します。
    # 設定可能な値: Goの time.Duration 形式の文字列（例: "5m", "1h"）
    # 省略時: デフォルトタイムアウト
    update = "5m"

    # delete (Optional)
    # 設定内容: リソース削除時のタイムアウト時間を指定します。
    # 設定可能な値: Goの time.Duration 形式の文字列（例: "5m", "1h"）
    # 省略時: デフォルトタイムアウト
    delete = "5m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: ロケールの複合 ID（bot_id:bot_version:locale_id 形式）
#---------------------------------------------------------------
