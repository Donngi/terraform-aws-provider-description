#---------------------------------------------------------------
# AWS VPC Security Group Rules Exclusive
#---------------------------------------------------------------
#
# セキュリティグループに割り当てるインバウンド・アウトバウンドルールの
# 排他的な管理を行うリソースです。
# このリソースで明示的に定義されていないルールは自動的に削除されるため、
# セキュリティグループのルール構成を完全に制御できます。
#
# 注意: このリソースはセキュリティグループのルールの排他的な所有権を持ちます。
#       aws_vpc_security_group_ingress_rule や aws_vpc_security_group_egress_rule で
#       管理しているルールIDを ingress_rule_ids / egress_rule_ids に含めてください。
#       含めない場合、それらのルールは削除されます。
#
# 注意: このリソースを destroy しても、セキュリティグループからルールは
#       削除されません。Terraformによる排他管理が解除されるだけです。
#
# AWS公式ドキュメント:
#   - セキュリティグループ: https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-groups.html
#   - セキュリティグループルール: https://docs.aws.amazon.com/vpc/latest/userguide/security-group-rules.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/vpc_security_group_rules_exclusive
#
# Provider Version: 6.36.0
# Generated: 2026-03-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_vpc_security_group_rules_exclusive" "example" {
  #-------------------------------------------------------------
  # 対象セキュリティグループ設定
  #-------------------------------------------------------------

  # security_group_id (Required)
  # 設定内容: 排他的にルールを管理するセキュリティグループのIDを指定します。
  # 設定可能な値: 有効なセキュリティグループID（例: "sg-12345678"）
  security_group_id = "sg-12345678"

  #-------------------------------------------------------------
  # ルールID設定
  #-------------------------------------------------------------

  # ingress_rule_ids (Required)
  # 設定内容: このセキュリティグループに割り当てるインバウンドルールIDのセットを指定します。
  #           ここに含まれないインバウンドルールは自動的に削除されます。
  #           空リストを指定すると全インバウンドルールが削除されます。
  # 設定可能な値: aws_vpc_security_group_ingress_rule のIDリスト（例: ["sgr-12345678"]）
  ingress_rule_ids = []

  # egress_rule_ids (Required)
  # 設定内容: このセキュリティグループに割り当てるアウトバウンドルールIDのセットを指定します。
  #           ここに含まれないアウトバウンドルールは自動的に削除されます。
  #           空リストを指定すると全アウトバウンドルールが削除されます。
  # 設定可能な値: aws_vpc_security_group_egress_rule のIDリスト（例: ["sgr-87654321"]）
  egress_rule_ids = []

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するAWSリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: "ap-northeast-1", "us-east-1"）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは設定した引数に加え、追加の属性をエクスポートしません。
#---------------------------------------------------------------
