#---------------------------------------------------------------
# Amazon Lex V2 Bot Version
#---------------------------------------------------------------
#
# Amazon Lex V2 のボットバージョンをプロビジョニングするリソースです。
# ボットバージョンは、ドラフト（$LATEST）バージョンのボットを
# 不変なスナップショットとして公開するために使用します。
# 公開されたバージョンはエイリアスに関連付けることで、
# 本番環境への安定したデプロイが実現できます。
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/lexv2models_bot_version
#
# Provider Version: 6.28.0
# Generated: 2026-02-17
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_lexv2models_bot_version" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # bot_id (Required)
  # 設定内容: バージョンを作成する対象のボット ID を指定します。
  # 設定可能な値: 既存の aws_lexv2models_bot リソースの bot_id 属性
  # 省略時: 省略不可
  bot_id = aws_lexv2models_bot.example.id

  # bot_version (Optional, Computed)
  # 設定内容: 作成するボットバージョンの番号を指定します。
  #           通常は Terraform が自動的に採番するため省略します。
  # 設定可能な値: 数値文字列（例: "1", "2"）
  # 省略時: Terraform が自動的に次のバージョン番号を割り当て
  bot_version = null

  # description (Optional)
  # 設定内容: ボットバージョンの説明を指定します。
  # 設定可能な値: 最大200文字の文字列
  # 省略時: 説明なし
  description = "Amazon Lex V2 ボットバージョンのサンプルテンプレートです。"

  #-------------------------------------------------------------
  # ロケール仕様設定
  #-------------------------------------------------------------

  # locale_specification (Required)
  # 設定内容: バージョンに含めるロケールとそのソースバージョンのマッピングを指定します。
  #           キーはロケール ID（例: "ja_JP", "en_US"）、値はソースボットバージョンを
  #           含むオブジェクトです。
  # 設定可能な値: ロケール ID をキーとするマップ。各ロケールに source_bot_version を指定します。
  #   - source_bot_version: バージョン作成の元となるボットバージョン（通常は "DRAFT"）
  # 省略時: 省略不可
  locale_specification = {
    "ja_JP" = {
      source_bot_version = "DRAFT"
    }
    "en_US" = {
      source_bot_version = "DRAFT"
    }
  }

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効な AWS リージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: Goの time.Duration 形式の文字列（例: "5m", "1h"）
    # 省略時: デフォルトタイムアウト
    create = "5m"

    # delete (Optional)
    # 設定内容: リソース削除時のタイムアウト時間を指定します。
    # 設定可能な値: Goの time.Duration 形式の文字列（例: "5m", "1h"）
    # 省略時: デフォルトタイムアウト
    delete = "5m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - bot_version: 作成されたボットの数値バージョン（例: "1", "2"）
# - id: リソースの一意識別子（bot_id と bot_version の組み合わせ）
#---------------------------------------------------------------
