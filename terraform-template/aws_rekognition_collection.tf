#---------------------------------------------------------------
# AWS Rekognition Collection
#---------------------------------------------------------------
#
# Amazon Rekognitionの顔コレクションをプロビジョニングするリソースです。
# コレクションはインデックス化された顔ベクトルを格納するコンテナで、
# 顔検索・顔認証・映像解析などのユースケースで使用します。
# コレクション自体は顔の画像を格納しているのではなく、
# 顔の数学的な特徴量（ベクトル）を格納します。
#
# AWS公式ドキュメント:
#   - Amazon Rekognition コレクション概要: https://docs.aws.amazon.com/rekognition/latest/dg/collections.html
#   - コレクション内の顔検索: https://docs.aws.amazon.com/rekognition/latest/dg/collections-search-faces.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/rekognition_collection
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_rekognition_collection" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # collection_id (Required, Forces new resource)
  # 設定内容: コレクションの一意識別子（名前）を指定します。
  # 設定可能な値: 英数字・アンダースコア・ハイフンを含む文字列
  # 参考: https://docs.aws.amazon.com/rekognition/latest/dg/collections.html
  collection_id = "my-collection"

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するAWSリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用します。
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タグ設定
  #-------------------------------------------------------------

  # tags (Optional)
  # 設定内容: リソースに割り当てるタグのマップを指定します。
  # 設定可能な値: キーと値のペアのマップ
  # 省略時: タグなし
  # 注意: プロバイダーレベルのdefault_tags設定ブロックで定義されたタグと
  #       一致するキーを持つタグは、プロバイダーレベルで定義されたものを上書きします。
  tags = {
    Name        = "my-collection"
    Environment = "production"
  }

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  timeouts {
    # create (Optional)
    # 設定内容: リソース作成時のタイムアウト時間を指定します。
    # 設定可能な値: "30s", "5m", "2h45m" などの期間文字列（s: 秒, m: 分, h: 時間）
    # 省略時: デフォルトのタイムアウト値を使用します。
    create = "30m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - arn: コレクションのAmazon Resource Name (ARN)
#
# - face_model_version: コレクションの初期化に使用された顔モデルのバージョン
#
# - tags_all: プロバイダーのdefault_tags設定ブロックから継承されたタグを含む、
#             リソースに割り当てられたすべてのタグのマップ
#---------------------------------------------------------------
