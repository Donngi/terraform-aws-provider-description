#---------------------------------------------------------------
# AWS VPC Route Table Association
#---------------------------------------------------------------
#
# Amazon VPCのルートテーブルをサブネットまたはインターネットゲートウェイ・
# 仮想プライベートゲートウェイに関連付けるリソースです。
# サブネットに関連付けたルートテーブルは、そのサブネットのトラフィックの
# ルーティングを制御します。ゲートウェイに関連付けたルートテーブル（ゲートウェイ
# ルートテーブル）は、VPCに入るトラフィックの制御に使用します。
#
# AWS公式ドキュメント:
#   - VPCルートテーブルの設定: https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html
#   - サブネットルートテーブルの管理: https://docs.aws.amazon.com/vpc/latest/userguide/WorkWithRouteTables.html
#   - ゲートウェイルートテーブル: https://docs.aws.amazon.com/vpc/latest/userguide/gateway-route-tables.html
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/route_table_association
#
# Provider Version: 6.28.0
# Generated: 2026-02-18
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_route_table_association" "example" {
  #-------------------------------------------------------------
  # 基本設定
  #-------------------------------------------------------------

  # route_table_id (Required)
  # 設定内容: 関連付けるルートテーブルのIDを指定します。
  # 設定可能な値: 有効なルートテーブルID（例: rtb-12345678）
  route_table_id = "rtb-12345678"

  #-------------------------------------------------------------
  # 関連付け対象設定
  #-------------------------------------------------------------

  # subnet_id (Optional)
  # 設定内容: 関連付けるサブネットのIDを指定します。
  # 設定可能な値: 有効なサブネットID（例: subnet-12345678）
  # 省略時: gateway_idを指定する場合は省略可能
  # 注意: subnet_idとgateway_idは排他的です。どちらか一方のみ指定してください。
  #       一つのサブネットに関連付けられるルートテーブルは1つのみです。
  #       関連するルートテーブルを変更した場合、サブネット内の既存の接続は切断されます
  #       （新しいルートテーブルが同じトラフィックへのルートを持つ場合を除く）。
  subnet_id = "subnet-12345678"

  # gateway_id (Optional)
  # 設定内容: 関連付けるインターネットゲートウェイまたは仮想プライベートゲートウェイのIDを指定します。
  # 設定可能な値: 有効なインターネットゲートウェイID（例: igw-12345678）または
  #               仮想プライベートゲートウェイID（例: vgw-12345678）
  # 省略時: subnet_idを指定する場合は省略可能
  # 注意: subnet_idとgateway_idは排他的です。どちらか一方のみ指定してください。
  #       ゲートウェイルートテーブルは、VPCに入るトラフィックをきめ細かく制御するために使用します。
  # 参考: https://docs.aws.amazon.com/vpc/latest/userguide/associate-route-table-gateway.html
  gateway_id = null

  #-------------------------------------------------------------
  # リージョン設定
  #-------------------------------------------------------------

  # region (Optional)
  # 設定内容: このリソースを管理するリージョンを指定します。
  # 設定可能な値: 有効なAWSリージョンコード（例: ap-northeast-1, us-east-1）
  # 省略時: プロバイダー設定のリージョンを使用
  # 参考: https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints
  region = null

  #-------------------------------------------------------------
  # タイムアウト設定
  #-------------------------------------------------------------

  # timeouts (Optional)
  # 設定内容: リソース操作のタイムアウト時間を設定するブロックです。
  timeouts {

    # create (Optional)
    # 設定内容: リソース作成操作のタイムアウト時間を指定します。
    # 設定可能な値: Goの時間文字列形式（例: "30s", "5m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    create = "5m"

    # update (Optional)
    # 設定内容: リソース更新操作のタイムアウト時間を指定します。
    # 設定可能な値: Goの時間文字列形式（例: "30s", "5m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    update = "5m"

    # delete (Optional)
    # 設定内容: リソース削除操作のタイムアウト時間を指定します。
    # 設定可能な値: Goの時間文字列形式（例: "30s", "5m", "1h"）
    # 省略時: プロバイダーのデフォルトタイムアウトを使用
    delete = "5m"
  }
}

#---------------------------------------------------------------
# Attributes Reference (読み取り専用属性)
#---------------------------------------------------------------
# このリソースは以下の属性をエクスポートします:
#
# - id: 関連付けのID
#---------------------------------------------------------------
