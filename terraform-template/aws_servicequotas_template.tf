#---------------------------------------------------------------
# AWS Service Quotas テンプレート
#---------------------------------------------------------------
#
# Service Quotasテンプレートを管理するリソースです。
# 組織の管理アカウントからのみ設定可能で、us-east-1リージョンで実行する必要があります。
# テンプレートに登録されたクォータは、組織に参加した新しいアカウントに自動適用されます。
#
# Terraform Registry:
#   - https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/servicequotas_template
#
# Provider Version: 6.28.0
# Generated: 2026-02-19
# NOTE: 本テンプレートは生成時点の情報に基づきAIが生成しています。
#       情報が古くなっている可能性、誤りを含む可能性があるため、
#       正確な最新仕様は公式ドキュメントを参照してください。
#
#---------------------------------------------------------------

resource "aws_servicequotas_template" "example" {

  #---------------------------------------
  # クォータ識別設定
  #---------------------------------------

  # 設定内容: クォータの識別子
  # 設定可能な値: AWSサービスのクォータコード文字列 (例: "L-2ACBD22F")
  # 省略時: 設定必須
  quota_code = "L-2ACBD22F"

  # 設定内容: サービスの識別子
  # 設定可能な値: AWSサービスコード文字列 (例: "lambda", "ec2", "s3")
  # 省略時: 設定必須
  service_code = "lambda"

  # 設定内容: クォータの新しい増加後の値
  # 設定可能な値: 現在のデフォルト値より大きい数値
  # 省略時: 設定必須
  value = 80

  #---------------------------------------
  # リージョン設定
  #---------------------------------------

  # 設定内容: テンプレートを適用するAWSリージョン
  # 設定可能な値: AWSリージョンコード文字列 (例: "us-east-1", "ap-northeast-1")
  # 省略時: プロバイダーのデフォルトリージョン
  aws_region = "us-east-1"

  # 設定内容: テンプレートを適用するAWSリージョン
  # 設定可能な値: AWSリージョンコード文字列
  # 省略時: aws_regionの値が使用される
  # 非推奨: aws_region を使用すること
  region = "us-east-1"
}

#---------------------------------------
# Attributes Reference
#---------------------------------------
# id          - リソースの一意識別子 (region, quota_code, service_codeをカンマ区切りで結合した文字列)
# global_quota - クォータがグローバルスコープかどうかを示すフラグ
# quota_name  - クォータ名
# service_name - サービス名
# unit        - クォータの測定単位
